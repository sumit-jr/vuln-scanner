services:

  backend:

    build: ./backend

    container_name: vuln-scanner-backend

    ports:
      - "10000:10000"

    restart: always


  frontend:

    build: ./frontend

    container_name: vuln-scanner-frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

    restart: always